# Dependencies
node_modules/
.DS_Store
*.swp
*.swo
*~

# Environment variables
.env
.env.local
.env.*.local

# Provider credentials & user-specific configs (NEVER commit!)
~/.config/kilocode/
~/.config/claude-code/
~/.config/copilot/
.claude/
.kilocode/
.copilot/
.qwen/
.gemini/

# IDE
.vscode/
.idea/
.cursor

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak
.tmp/

# Project-specific (users should not commit their actual project configs with secrets)
projects/*/config.local.json

# Private workspace config
.projects.local.md

# Panel
panel/node_modules/
panel/dist/
panel/panel.config.local.ts
panel/.autosync-state.json

# Per-host launcher overrides (e.g. PANEL_EXTRA_PORTS)
scripts/start-panel-windows.local.env

# Per-user agent command wrappers + local opencode config (generated by setup:*)
.opencode/
opencode.json

# agent-native / visual-docs scratch (generated locally, not source)
.agent-native/
agent-native.json
plan-blocks.md
